function hovermenuul(id) {
    const tag = document.getElementById(id);
    tag.classList.add('link-border')
}

function outmenuul(id) {
    const tag = document.getElementById(id);
    tag.classList.remove('link-border')
}


